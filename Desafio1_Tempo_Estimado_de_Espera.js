const nomeRestaurante = gets('Giraffas');
const tempoEstimadoEntrega = parseInt(gets(35));

const mensagem = `O restaurante ${nomeRestaurante} entrega em ${tempoEstimadoEntrega} minutos.`;
print(mensagem);